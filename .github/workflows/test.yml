name: Test Workflow
on: push

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Repository Code
              uses: actions/checkout@v4
            - name: Install NodeJS
              uses: actions/setup-node
              with:
                node-version: '20' 